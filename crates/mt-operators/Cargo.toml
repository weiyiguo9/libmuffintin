[package]
name = "libmuffintin-operators"
description = "Generic Hermitian operator assembly and overlap-filtered eigensolver"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[lib]
name = "muffintin_operators"

[dependencies]
faer = { version = "0.22.6", default-features = false, features = ["std", "linalg"] }
libmuffintin-core = { path = "../mt-core" }
libmuffintin-envelope = { path = "../mt-envelope" }
libmuffintin-sphere = { path = "../mt-sphere" }
libmuffintin-tensor = { path = "../mt-tensor" }
num-complex.workspace = true
thiserror.workspace = true

[lints]
workspace = true
