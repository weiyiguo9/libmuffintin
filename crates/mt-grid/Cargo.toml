[package]
name = "libmuffintin-grid"
description = "Typed atom-centred, uniform, and interstitial quadrature grids"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[features]
default = []
rstsr = ["dep:rstsr"]

[dependencies]
libmuffintin-core = { path = "../mt-core" }
rstsr = { version = "0.7.10", optional = true, default-features = false, features = ["std"] }
thiserror.workspace = true

[lints]
workspace = true
