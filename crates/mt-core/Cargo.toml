[package]
name = "libmuffintin-core"
description = "Conventions and closed-form numerical primitives for muffin-tin methods"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[lib]
name = "muffintin_core"

[features]
default = []
rstsr = ["dep:rstsr"]

[dependencies]
num-complex.workspace = true
rstsr = { version = "0.7.10", optional = true, default-features = false, features = ["std"] }
thiserror.workspace = true

[dev-dependencies]
proptest.workspace = true

[lints]
workspace = true
