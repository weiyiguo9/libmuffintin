[build-system]
requires = ["maturin>=1.9,<2.0"]
build-backend = "maturin"

[project]
name = "libmuffintin"
version = "0.2.0"
requires-python = ">=3.10"
dependencies = ["numpy"]

[tool.maturin]
manifest-path = "../crates/mt-python/Cargo.toml"
python-source = "."
module-name = "libmuffintin._native"
